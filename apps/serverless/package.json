{
  "name": "@sp3ndly/serverless",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "check": "node --check api/parse-receipt.js",
    "check-types": "node --check api/parse-receipt.js",
    "test": "node --test 'test/*.test.mjs'",
    "dev": "npx --yes vercel dev",
    "deploy": "npx --yes vercel --prod"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "cheerio": "^1.0.0"
  }
}