# Публичный адрес serverless-функции (Vercel). Коммитится в репозиторий намеренно:
# URL публичный, не секрет, и нужен CI-сборке на GitHub Actions
# (.env игнорируется git'ом, поэтому именно этот файл подхватывает `vite build`).
# Адрес — production-алиас проекта bashirovarthur-2383/serverless.
VITE_API_URL=https://serverless-tawny-xi-39.vercel.app